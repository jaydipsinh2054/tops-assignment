function App() {
  const message = "JSX allows writing HTML-like code inside JavaScript.";

  return (
    <div>
      <h1>Welcome to JSX</h1>
      <p>{message}</p>
    </div>
  );
}

export default App;